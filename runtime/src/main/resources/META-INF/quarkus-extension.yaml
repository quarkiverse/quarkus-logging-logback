artifact: ${project.groupId}:${project.artifactId}:${project.version}
name: Logging Logback
description: logback.xml support for logback appenders
metadata:
  keywords:
    - logging
    - logback
  categories:
    - "logging"
  status: "experimental"